import { AngularWebpackModule } from './angular-webpack.module';

describe('AngularWebpackModule', () => {

  it('should create', () => {
    expect(new AngularWebpackModule()).toBeTruthy();
  });
});
